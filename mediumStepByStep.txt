Username Checks:
    Cannot be 4 or less characters
    All characters must either be lowercase letters or the '@' or '.' symbols
    Must contain the '@' symbol, but cannot be all '@' symbols

Password Checks: 
    Can only contain digits
    Must have exactly 10 digits
    The digits must add up to 60